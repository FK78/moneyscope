DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
# Set to your production URL in deployed environments (e.g. https://flowdget.fahadkhan.me)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: Email alerts via Resend (https://resend.com)
RESEND_API_KEY=
RESEND_FROM_EMAIL=MoneyScope <alerts@yourdomain.com>

# TrueLayer Open Banking (https://truelayer.com)
# Set TRUELAYER_SANDBOX=true for sandbox mode during development
TRUELAYER_CLIENT_ID=
TRUELAYER_CLIENT_SECRET=
TRUELAYER_SANDBOX=true

# Encryption key for sensitive data (32 bytes, hex-encoded)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=